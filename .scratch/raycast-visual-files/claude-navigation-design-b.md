Read `.scratch/raycast-visual-files/claude-navigation-mockup-prompt.md` completely and follow it.

Create exactly `prototypes/navigation-search-preview/design-b-command.html`. Edit no other file.

Design B is the command-first interpretation. The normal Browse surface should remain calm and sparse, while activating Navigation Input opens a prominent floating command sheet with Search Results, paths, source hints, and keyboard affordances. The sheet must still obey the resolved separate-layer and Reveal contract. Tabs should be compact and may share one line with global controls. The result should feel closer to a purpose-built launcher fused with a file browser than to Finder or a conventional browser.

Do not reuse Design A's full-width stacked browser chrome. Do not turn it into generic Raycast imitation.

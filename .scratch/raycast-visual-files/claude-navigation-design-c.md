Read `.scratch/raycast-visual-files/claude-navigation-mockup-prompt.md` completely and follow it.

Create exactly `prototypes/navigation-search-preview/design-c-workbench.html`. Edit no other file.

Design C is the spatial workbench interpretation. Treat Locations and Tabs as persistent working contexts, with stronger path hierarchy and a more structural relationship between the dense file list and fixed Preview Panel. A slim vertical context rail or another non-browser arrangement is allowed if it makes this design genuinely different. Navigation Input may expand from a compact control into its Search Results layer. Keep the fast path dense and keyboard-first, but make the current Location and the Preview Panel feel more grounded than in a launcher.

Do not reuse Design A's browser stack or Design B's centered command sheet. Avoid looking like a code editor file tree.

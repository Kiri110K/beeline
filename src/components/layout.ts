// Fixed row height powers the hand-rolled windowing math.
export const ROW_HEIGHT = 28;
export const OVERSCAN = 8;

// Shared grid so the header and every row keep their columns aligned.
export const GRID_COLS =
  "grid grid-cols-[minmax(0,1fr)_5rem_9.5rem_6rem] items-center gap-2 px-3";
